{
  "name": "football-player-simulator",
  "version": "1.0.0",
  "scripts": {
    "sync": "node scripts/copy-web.mjs",
    "cap:build": "npm run sync && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "fs-extra": "^11.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
